# Backend only. Never put an OpenAI key into Android settings or source.
OPENAI_API_KEY=
PORT=8787
# Optional: reuse an existing ignored secret file instead of copying its key.
# SWITCHBOARD_SECRET_FILE=/absolute/path/to/existing/.dev.vars
